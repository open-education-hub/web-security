version: "3.3"

services:
  database:
    image: mysql:5.7
    environment:
      MYSQL_DATABASE: "demo"
      MYSQL_USER: "sss"
      MYSQL_PASSWORD: "sss"
      MYSQL_ROOT_PASSWORD: "root"

    volumes:
      - "../src/mysql/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql"

  web:
    build: .
    ports:
      - "8083:80"
    volumes:
      - ../src:/var/www/html
    links:
      - database
